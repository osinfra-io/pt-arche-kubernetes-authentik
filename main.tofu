# This module is consumed via its //regional and //regional/config sub-paths.
# The Authentik Helm release lives in regional/; the Authentik provider objects
# (OIDC provider, application, proxy forward-auth provider, scope mappings, and the
# proxy-provider assignment to the built-in embedded outpost) live in regional/config/.
